# 保留リスト（やることの控え）

今すぐ着手しない／後回しにする項目の控え。着手・解消したらここから外す。

## OGREF_library
- [ ] **カスタムテーマの保存（複数保存）**
  - 現状: オリジナル（カスタム）テーマは「アクセント色＋背景色」を1つ設定して即適用できるが、名前を付けて複数保存・再選択する仕組みはない。
  - やりたいこと: 作ったカスタムテーマを名前付きで保存し、テーマ一覧（プリセットと並べて）から選び直せるようにする。
  - 補足検討: 保存先（ライブラリ単位 / ワークスペース単位 / ユーザー単位）、上限、削除・リネーム、観覧URLへの反映。
- [ ] 文字色の手動指定（テーマの自動生成に加えて任意で上書き）
- [ ] 16進コードの直接入力欄（カラーピッカーに加えて）
- [ ] 観覧URLの per-library ディープリンク（`?view=token&lib=ID`）
- [ ] レイアウトの高度化（タイルの行スパン/列スパン、テキスト装飾 など）
- [ ] モバイル最適化（観覧/編集とも現状はPC前提）
- [ ] 本番（`lib_`）への展開（dev検証後）
- [ ] ビルド確認用ラベル（APP_BUILD）の最終的な撤去
- [ ] 新ロゴの適用

## OGREF 本体 / 共通基盤
- [ ] **タイル自動再生のプリバッファ（先読み）**。`IntersectionObserver` の `rootMargin` を広げ、タイルが画面に入る手前でiframeを生成→表示時には再生済みにし、YouTube等の再生開始ラグ（プレイヤー初期化・バッファリング）を体感ゼロに近づける。負荷増とのトレードオフ・先読み量の調整が必要。（load後の固定1000ms表示遅延は撤去済み）
- [ ] **「新しいフォルダ」ボタンを廃止したい**（フォルダ作成導線の見直し）。※解析のみ済（推奨案: 追加モーダルにフォルダ作成を内包＋手動フォルダは空でも表示。空フォルダ作成は実装済み）。
- [ ] **YouTube等からダイレクト追加の続き** — PCブックマークレット（`#add=URL`受け口＋追加先WS選択）は実装済み。残りは:
  - [ ] PWA化＋モバイルWeb Share Target（共有シートからOGREFへ送って追加）。manifest.json(share_target)＋Service Worker＋アイコンが必要。※iOS SafariはWeb Share Target非対応のためiOSはショートカットで補完。Beta反映前にdevで検証。
  - [ ] 完全自動登録（保存も無確認）にしたい場合はChrome拡張機能。
- [ ] **連番画像（イメージシーケンス）対応**。※検討中・未着手。
  - 現状: ローカル取り込みは動画ファイルのみ（`_videoExts=['mp4','mov','avi','mkv','webm','m4v','wmv','flv']`／`OGREF_dev.html:3238`）。`.gif`/`.png`/`.jpg` 等はスキャンで除外され、再生は `<video>` 要素のみ。
  - GIF（手軽）: `_videoExts` に `gif` 等を追加＋ローカル描画を「動画→`<video>` / 画像→`<img>`」に分岐すればタイル表示・自動ループ可能。サムネ・自動再生もほぼ流用可。
  - PNG等の連番（重め・新機能）: フォルダ内の連番（例 `shot_0001.png`…）を1素材にまとめ、`requestAnimationFrame`/canvas で **fps指定の擬似再生プレイヤー** を実装する必要。fps指定・コマ送り・先読み（プリロード）の設計が要る。
  - 進め方案: まず GIF 対応を dev で先行（効果が分かりやすい）→ 連番は別途検討。
- [ ] Firestore のデータ蓄積（孤児データのGC・クリーンアップ）。※ローカルフォルダ分（実体の無いローカル動画/空フォルダ/重複フォルダ）は実装済み。残りは非ローカル領域（削除済みWSの残骸 等）の掃除。
- [ ] サーバー負荷 / Firebaseコスト（ページネーション、非アクティブlistener停止 など）
- [ ] **デスクトップアプリ化（ローカル起動ソフト）**。※方針のみ・未着手。
  - 構成: **Electron**（中に Chromium を内蔵し、今のHTMLをそのまま表示）。**サーバーはブラウザ版と完全共通**＝同じ Firebaseプロジェクト（`animref-ef532`）/ Firestore（同一DB）/ Google認証 / Security Rules を共有。ブラウザ版ユーザーとデスクトップ版ユーザーは同じデータをリアルタイム共有する。別DB・同期処理は不要。
  - コード変更は最小: `firebaseConfig` は現状のまま、`DEV_MODE`（`dev_*`/prod）もそのまま流用可（dev用/本番用デスクトップを同じ作りで切替可）。
  - メリット: Electronなら Node.js の `fs` が使え、File System Access API の弱点（フルパス取得不可＝★7の末尾名照合問題、起動ごとの再許可）を根本解消できる（デスクトップ版だけの上積み）。
  - 制約: Firebaseクラウド共有のため**オフライン不可（ネット必須）**。配布形式（.exe/.dmg）のビルド・自動更新の仕組みは別途必要。
  - 着手時の進め方: まず dev 向けに最小構成のElectronラッパー（`OGREF_dev.html` を読むだけ）から。
- [ ] Beta への反映（dev で蓄積した変更）※ユーザーが明示するまで実施しない
- [ ] **Betaユーザーのワークスペースデータのバックアップ → サーバークリーンアップ → 再構築**（保留）。
  - 目的: 本番（`workspaces`/`users`/`access_control`/`feedback` ＋ サブコレクション `videos`・`folders`・`members`）のバックアップを取得し、一度クリーンアップしてから再構築する。
  - 方式案: ①Firebase標準の `gcloud firestore export/import`（最も安全・推奨／ユーザー操作）、②運営コンソールにJSONエクスポート/インポート＋一括削除機能を実装（私が実装可・操作はユーザー）。
  - 注意: 削除は取り消せないため「バックアップ→中身確認→削除」の順。何を残し何を消すか（全WS / 特定ユーザーのみ / アクセス権設定維持 等）の確定が必要。
- [ ] **招待リンク（承認不要）の Beta 反映**。dev＋運営コンソールには実装済み（共有リンク・無期限・手動無効化）。Beta（本番アプリ）側の `#invite=` 自動許可は未実装＝保留。コンソールは本番envでも発行可だが、その旨を注記表示中。

## OGREF_Canvas（自由配置版・PureRef風 / 開発中）
`OGREF_dev.html` をフォークした新アプリ。**サーバー/データは dev と共通**（同じ `dev_workspaces`/`dev_*`）。各動画ドキュメントに `canvas:{x,y,w,z}`（共有レイアウト）を保存し、無限キャンバスへ自由配置する。グリッド版（OGREF_dev/Beta）はこの `canvas` フィールドを無視するため相互に影響しない。
- 実装済み（v0.1.0 / CANVAS_v0.1.0）: パン（空きスペースのドラッグ）・カーソル基準ズーム（ホイール）・タイルのドラッグ移動・右下ハンドルでリサイズ・選択/前面化（z順）・ダブルクリックで右パネル表示・全体表示(fit)/100%・自動再生（既存IntersectionObserver＋通信量抑制を流用）・配置のFirestore保存（移動/リサイズ時）。認証/WS/サイドバー/追加/ローカルフォルダは dev から流用。
- v1の制約・今後（未着手）:
  - [ ] **リモートのレイアウト変更がライブ反映されない**（他ユーザーの移動は各自リロードで反映＝グリッド版の方針に合わせた。ライブ同期は要検討）。
  - [ ] **ズームアウト時に画面外でも全タイルのiframeが生成され得る**（負荷）。表示中タイルのビューポート間引き（zoom連動カリング）。
  - [ ] 回転、複数選択、整列/スナップ、グリッド整列ボタン、Undo/Redo。
  - [ ] レイアウトを「共有」ではなく「ユーザーごと」に切替える選択肢。
  - [ ] モバイル最適化（現状PC前提。タッチのピンチズーム等）。
  - [ ] 不要になった旧 `makeCard`/グリッド専用コードの整理（現状は無害なデッドコードとして残置）。

## OGREF 解析で判明した不具合・歪み（未着手・要修正 / 2026-06-08 解析）
コード解析（OGREF_dev.html）で確認。★=今回の解析で発見、◇=既存項目と同根。

### 🔴 バグ（今すぐ直したい）
- [ ] **★1 `leaveWorkspace` が二重定義**（1623行=id受取でリストから削除 / 1769行=引数なしで現在WSを退出）。後者が前者を上書きし、WS一覧の「✕ リストから削除」が `leaveWorkspace(ws.id)` を呼んでも**idが無視**される → 現在WS対象に動く/何も起きない。**オーナーだと現在WSを完全削除しかねない**。最優先。
- [ ] **★2 `openPlayer` が二重定義（デッドコード）**（2162行=旧モーダル実装 / 3751行=現右パネル実装）。後者のみ有効で前者は未使用。機能影響なしだが「直しても効かない」保守の罠。旧定義を削除。

### 🟠 データの歪み（孤児・蓄積）
- [ ] **★3 WS完全削除で `members` サブコレクションが残る**（videos/folders/WS本体は削除するが members/{uid} 未削除）。
- [ ] **★4 WS削除後、他メンバーの参加WSリストに残骸**（削除者の一覧からは消えるが、他ユーザーの `users/{uid}/…/{wsId}` 参照が残り、開くと「取得失敗」のダングリング）。
- [ ] ◇5 非ローカル領域の孤児GC全般（「Firestoreのデータ蓄積」項目と同根。ローカルフォルダ分は整合済み）。

### 🟡 ローカル整合の残リスク
- [ ] **★6 ベースパス未設定だと誤接続ガードが無効**（`folderMatchesBase()` が null → 名前一致ガードを素通り → 別フォルダ接続で共有docを一括削除しうる）。
- [ ] **★7 フォルダ名一致は「末尾名」だけ照合**（ブラウザ仕様でフルパス取得不可）。同名の別フォルダで誤通過の可能性（残存リスク）。

### 🔵 パフォーマンス／コスト
- [ ] **★8 リロード整合で `getDocs` を毎回複数発行**（refreshLocalFolder内で videos×2＋folders＋dedup の全件読み）。大規模WS・多人数で読み取りコスト/遅延増（◇「サーバー負荷」と同根）。
- [ ] **★9 空 `catch(){}` が31か所**。失敗（権限エラー等）が握りつぶされ見えにくい（観測性低下）。

### ⚪ 小さな歪み
- [ ] **★10 「各自リロードのみ反映」とサイドバーの不整合**。グリッドは固定だがサイドバーのカウント(badge)はリモート変更で更新→一瞬グリッドと数が合わない（リロードで一致。実害小）。

## プロジェクト分離方針（商用化に伴う環境/プロダクト分離）
商用化（サブスク）を見据えた **Firebaseプロジェクトの分け方** の方針。「テナントはプール型で単一DB」は“テナント間”の話で、こちらは“環境/プロダクト間”の話（別軸・矛盾しない）。

### ロードマップ
1. まず **現行の個人向けOGREF をサブスク商用化**。
2. その後、**法人向け（マルチテナント版）を展開**。

### 決定事項
- **商用化したら dev と prod は「プロジェクト単位」で分離する**（現状の `dev_`/`lib_dev_` プレフィックス分離は商用本番の保護にならない）。
  - 最大理由: **Security Rules はプロジェクトに1つだけ**。同一プロジェクトだと dev のルール変更・誤操作・負荷試験が**課金本番に波及**する。
  - 他にも: 請求/クォータの分離、バックアップ/PITRを本番のみ、App Check/承認ドメインを本番だけ厳格化、IAMで本番を少人数に限定、などが可能になる。
- **法人向けマルチテナント版は、個人向けとは別プロジェクト**（収益プロダクトの境界＝プロジェクト境界。課金・障害影響範囲・セキュリティ/ガバナンスの分離のため）。各プロジェクト内は `organizations/{orgId}` のプール型。

### 推奨プロジェクト構成（最終像）
```
個人サブスク版 : prod = animref-ef532（実データありのため継続） / 新規 ogref-dev（開発）/ 任意 ogref-staging
法人MT版(後)   : ogref-saas-prod / ogref-saas-dev（プール型 orgId で全テナント収容）
```
- 既存 `animref-ef532` は本番として継続し、**dev を新プロジェクトへ切り出す**のが移行コスト最小。

### 必要なコード変更（着手時）
- `DEV_MODE` を「コレクション名プレフィックス切替」から **`firebaseConfig`（接続先プロジェクト）切替** に変更。
- プロジェクトが分かれれば collection 名はクリーンになり、`dev_`/`lib_dev_` プレフィックスは将来廃止可能（移行中は併用可）。
- ※ Firebaseプロジェクトの新規作成・APIキー発行はFirebaseコンソールでのオーナーWORK。作成後に config を貼れば組込み可能。

## 会社用アプリ（マルチテナント＋サブスク）
**目的**: サブスク販売。小規模プラン=5人 / 大規模プラン=30人。会社が購入し、プランごとに使えるユーザー数を制限。

### 方針（検討結果）
- **テナント分離は「プール型（単一プロジェクト・単一DB）＋ orgId パススコープ」を採用**。
  - 完全分離（会社ごとにプロジェクト/named DB）は多数の中小テナントには非現実的（スケール/運用/コスト/横断操作の観点）。法的データ所在地や特定大企業の契約要件が出たら、その顧客だけハイブリッドで分離検討。
  - **コレクション名プレフィックス（例 `acme_workspaces`）はテナント分離に不適**。`dev_`/`lib_` は「アプリ/環境分離」用であり別物。テナントは **パス階層** `organizations/{orgId}/…` で分ける。
  - 分離強制は **Security Rules ＋ Auth Custom Claims（トークンに orgId/role/plan）**。`request.auth.token.orgId == orgId` で他社データを完全遮断＝SaaS標準の分離レベル。

### データモデル案
```
organizations/{orgId} = { name, plan:'small'|'large', seatLimit:5|30,
                          seatUsed, status:'active'|'suspended', ownerUid, createdAt }
organizations/{orgId}/members/{uid} = { email, role:'admin'|'member', addedAt }  // 1件=1席
organizations/{orgId}/workspaces/{wsId}/...   // WSを組織配下に
users/{uid} = { orgId, role }                 // 所属組織の逆引き
```

### 席数制限は3層で強制（クライアントのみは改ざん可なので不可）
1. クライアント: 追加時に `seatUsed < seatLimit` を確認（UX）。
2. Firestoreルール: `seatUsed` カウンタ＋トランザクションで上限超過の create を拒否。`status=='active'` も条件。
3. Cloud Functions: 課金(Stripe等)Webhookで `plan/seatLimit/status` を更新。席超過・課金整合の最終防衛。

### 注意点
- **ドメイン許可（@company.com 全員）と席数制限は相性が悪い**（人数青天井）。席で売るなら個別招待制を基本に。
- サブスク＝**Cloud Functions（サーバー）追加が前提**。現状は純クライアント構成のため、ここが最大の追加要素。
- 現状は単一テナント（`access_control/config` が1つ）。マルチテナント化＝これを org 単位に分割し `checkAccess` を組織解決＋席数チェックへ改修。

### 進め方（段階導入）
- フェーズ1: `organizations`/`members` 導入、`checkAccess` 改修、組織作成・プラン設定は運営が手動登録、課金は手動請求。→ これだけで「5人/30人制限」は機能（ルール強制込み）。
- フェーズ2: Stripe Billing 接続（購入→組織自動発行→プラン/席数/解約の自動反映）＋ Custom Claims 導入で完全自動化。

## セキュリティ（要検討）
- [ ] 観覧ミラー（`lib_*_public`）は認証なしで読めるため `pwHash` がブラウザから見え得る。高機密用途には非推奨。本格対応は Cloud Functions 等でのサーバー側検証が必要。

---
## 解消済み（dev検証で対応・参考記録）
- ✅ **アクセス権の管理まわり（運営コンソール / 2026-06）**:
  - 削除が効かない不具合の修正（旧スキーマの単数 `adminEmail` が削除時に未クリアで再表示されていた → `deleteField` で除去。admin・dev 両UI）。
  - 別ウィンドウのアクセス管理で削除ボタンが無反応な不具合の修正（`confirm` が親ウィンドウ側で抑制されていた → 操作中ウィンドウに出す `uiConfirm` を追加）。
  - 承認不要の**招待リンク**（自動メンバー登録／共有・無期限・手動無効化）を dev＋運営コンソールに追加（Betaは保留）。
  - 運営コンソールで**アカウント名（表示名）を表示**（dev・本番両方の `loggedUsers` ＋ 申請の名前 ＋ ログイン中の自分から収集。未ログインのユーザーはメールのみ＝情報が無いため）。
  - 要Firestoreルール追記: `(dev_)access_requests`・`(dev_)access_invites` の read/write 許可。
- ✅ タグ未設定の動画だけを表示するフィルタ（「タグなし」）
- ✅ YouTube等からのダイレクト追加 — PCブックマークレット（`#add=URL`＋追加先WS選択）
- ✅ ローカルフォルダの歪み解消（空フォルダ作成可 / 同名フォルダ重複防止 / 実体の無いローカル動画・空フォルダの孤児GC）
- ✅ ad-hoc改善: タイルサイズ切替（小5/中4/大3列・個人設定）、Google Drive のタイル表示・自動再生改善、X/IG/TikTok埋め込みのサイズ変更ズレ修正、ローカルのベースパスにコピーボタン、Firestoreに名前付きユーザープロフィール（`users/{uid}`）
- ✅ 開きっぱなし時の通信量抑制（Beta_v0.2.3反映済 / DEV_v0.5.3）: ①タブが非表示になったら自動再生を一時停止し再生中のiframe/動画を破棄＝ストリーミング停止、②無操作5分でサムネ表示へ自動切替（「5分間操作が確認されなかったため自動再生を停止中」バナー表示）。タブに戻る/画面操作で自動再生へ復帰。保存設定`autoplayEnabled`は変更しない一時停止（`playbackSuspended`）。併せて自動再生のラグ低減（load後固定1000ms表示遅延の撤去）もBeta反映。今回は更新情報の初回自動表示なし（`PATCH_AUTOSHOW=false`）。

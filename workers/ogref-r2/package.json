{
  "name": "ogref-r2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "OGREF R2 backend Worker: signed upload / signed view URLs with Firebase ID token auth (dev)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
